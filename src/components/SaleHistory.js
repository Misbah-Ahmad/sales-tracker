import { Typography, Card, Col, Row } from "antd";

const SaleHistory = ({ includeForm }) => {
  const { Text } = Typography;
  return (
    <Card style={{ margin: '20px auto' }} headStyle={{fontWeight: 700}} title="Sales Report">

    

    </Card>
  );
};

export default SaleHistory;
